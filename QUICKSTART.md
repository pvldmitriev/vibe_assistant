# 🚀 Быстрый старт Vibe Assistant

## Предварительные требования

- Docker и Docker Compose установлены
- OpenAI API ключ (получите на https://platform.openai.com/api-keys)

## Шаг 1: Настройка окружения

1. Создайте `.env` файл:
```bash
cp .env.example .env
```

2. Отредактируйте `.env` и добавьте ваш OpenAI API ключ:
```env
OPENAI_API_KEY=sk-your-actual-api-key-here
```

## Шаг 2: Запуск приложения

### Режим разработки (с hot-reload)

```bash
npm run dev
```

Или через Docker Compose:
```bash
docker-compose -f docker-compose.dev.yml up
```

### Production режим

```bash
# Сборка образов
npm run build

# Запуск контейнеров
npm start
```

## Доступ к приложению

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001
- **Health Check**: http://localhost:3001/health

## Как использовать

1. Откройте http://localhost:3000 в браузере
2. Введите описание вашей идеи (минимум 20 символов)
3. Нажмите "Анализировать идею"
4. Изучите предложенный образ продукта
5. Нажмите "Принять и создать план" или скорректируйте образ
6. Получите пошаговый план разработки
7. Для каждого шага:
   - Прочитайте описание
   - Скопируйте промпт (кнопка "Копировать")
   - Вставьте промпт в Cursor или Replit
   - Выполните инструкции
   - Проверьте Definition of Done
   - Отметьте шаг как выполненный

## Остановка приложения

```bash
npm run stop
```

Или:
```bash
docker-compose down
```

## Полная очистка (включая volumes)

```bash
npm run clean
```

## Тестирование API

Запустите тестовый скрипт:
```bash
./scripts/test-api.sh
```

## Устранение неполадок

### Ошибка "OPENAI_API_KEY not found"
- Убедитесь, что файл `.env` существует
- Проверьте, что ключ правильно указан
- Перезапустите контейнеры

### Порты заняты
- Убедитесь, что порты 3000 и 3001 свободны
- Измените порты в `docker-compose.yml` если нужно

### Контейнеры не запускаются
- Проверьте логи: `docker-compose logs`
- Убедитесь, что Docker Desktop запущен

## Структура проекта

```
vibe_assistant/
├── backend/           # Node.js/Express API
├── frontend/          # Next.js приложение
├── .cursor/rules/     # Правила для Cursor AI
├── docs/              # Документация
├── scripts/           # Вспомогательные скрипты
└── docker-compose.yml # Docker конфигурация
```

## Следующие шаги

1. Изучите файлы правил в `.cursor/rules/` для понимания архитектуры
2. Прочитайте `README.md` для детальной документации
3. Начните создавать свой первый проект!

## Поддержка

При возникновении проблем:
1. Проверьте логи контейнеров
2. Убедитесь, что все зависимости установлены
3. Проверьте, что OpenAI API ключ действителен

