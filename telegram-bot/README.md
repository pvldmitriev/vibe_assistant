# 🤖 Telegram Bot для Vibe Assistant

Telegram бот для создания MVP продуктов с помощью AI.

## Возможности

- 💡 Анализ идеи через AI
- 📦 Генерация образа MVP продукта
- 📋 Создание пошагового плана разработки
- ✅ Отслеживание прогресса выполнения шагов
- 📝 Готовые промпты для IDE (Cursor/Replit)

## Команды бота

- `/start` - Начать работу с ботом
- `/newproject` - Создать новый проект
- `/myprojects` - Мои проекты (в разработке)
- `/cancel` - Отменить текущую операцию
- `/help` - Справка

## Настройка

### 1. Создание бота

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

### 2. Конфигурация

Создайте `.env` файл:

```bash
cp .env.example .env
```

Добавьте ваш токен:

```env
BOT_TOKEN=your_bot_token_here
BACKEND_URL=http://backend:3001
```

### 3. Запуск

#### Через Docker (рекомендуется)

```bash
# Из корня проекта
docker-compose up telegram-bot
```

#### Локально

```bash
cd telegram-bot
npm install
npm start
```

## Архитектура

```
telegram-bot/
├── bot.js                 # Главный файл бота
├── src/
│   ├── handlers/          # Обработчики команд и событий
│   │   ├── commandHandler.js    # Команды (/start, /help, и т.д.)
│   │   ├── ideaHandler.js       # Обработка идей
│   │   └── planHandler.js       # Работа с планами
│   ├── services/
│   │   └── apiClient.js         # Клиент для Backend API
│   └── utils/
│       └── formatter.js         # Форматирование сообщений
├── Dockerfile
├── Dockerfile.dev
└── package.json
```

## Использование

### 1. Создание проекта

1. Напишите `/start` или нажмите кнопку "Начать"
2. Опишите вашу идею (минимум 20 символов)
3. Дождитесь анализа от AI

### 2. Работа с образом продукта

- Нажмите "✅ Принять" чтобы создать план
- Или "✏️ Скорректировать" для уточнений

### 3. Выполнение плана

1. Просмотрите список шагов
2. Нажмите на шаг для просмотра деталей
3. Скопируйте промпт в IDE
4. Выполните инструкции
5. Отметьте шаг как выполненный

## API Integration

Бот использует Backend API:

- `POST /api/analyze-idea` - Анализ идеи
- `PUT /api/analyze-idea/:id` - Корректировка образа
- `POST /api/generate-plan` - Генерация плана
- `GET /api/steps/:projectId` - Получение шагов
- `POST /api/steps/:stepId/complete` - Отметка шага

## Технологии

- **telegraf** - фреймворк для Telegram ботов
- **axios** - HTTP клиент
- **dotenv** - управление переменными окружения

## Разработка

```bash
npm run dev  # Запуск с hot-reload
```

## Troubleshooting

### Бот не отвечает

1. Проверьте что токен правильный
2. Убедитесь что backend запущен
3. Проверьте логи: `docker-compose logs telegram-bot`

### Ошибка подключения к backend

Убедитесь что:
- Backend контейнер запущен
- `BACKEND_URL` правильно настроен
- Контейнеры в одной Docker сети

### Бот отвечает медленно

- AI запросы могут занимать 5-15 секунд
- Проверьте интернет соединение
- Убедитесь что OpenAI API ключ действителен

## Лицензия

MIT

